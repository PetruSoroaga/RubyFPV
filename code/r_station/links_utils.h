#pragma once
#include "../base/base.h"
#include "../base/models.h"

bool links_set_cards_frequencies_for_search( u32 iSearchFreq );

void send_alarm_to_central(u32 uAlarm, u32 uFlags, u32 uRepeatCount);
